import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import ButtonIconCondensed from "../components/ButtonIconCondensed";
import Button from "../components/Button";

<Meta
  title="ButtonIconCondensed"
  component={ButtonIconCondensed}
  subcomponents={{ Button }}
/>

export const Template = (args) => <ButtonIconCondensed {...args} />;

# 🔵 ButtonIconCondensed

A `<ButtonIcon />` extended to not have padding. Note that the `accessibilityLabel` prop is required.`

## Usage

```jsx
import ButtonIconCondensed from 'components/shared/DesignSystem/SlabTransferPortal/Buttons/ButtonIconCondensed';

---

<ButtonIconCondensed accessibilityLabel="Hi" color="primary" iconName="account-multiple" />
```

## Props Playground

<Canvas>
  <Story
    name="default"
    args={{
      accessibilityLabel: "More information",
      size: "medium",
      color: "subtle",
      iconName: "information",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
