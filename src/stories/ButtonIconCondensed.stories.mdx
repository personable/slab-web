import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import ButtonIconCondensed from "../components/ButtonIconCondensed";
import Button from "../components/Button";

<Meta
  title="ButtonIconCondensed"
  component={ButtonIconCondensed}
  subcomponents={{ Button }}
/>

export const Template = (args) => <ButtonIconCondensed {...args} />;

# ButtonIconCondensed

A ButtonIcon extended to not have padding.

<Canvas>
  <Story
    name="default"
    args={{
      accessibilityLabel: "More information",
      size: "medium",
      color: "subtle",
      iconName: "info",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
