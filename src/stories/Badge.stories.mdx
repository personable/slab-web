import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import Badge from "../components/Badge";
import Button from "../components/Button";
import Spinner from "../components/Spinner";

<Meta title="Badge" component={Badge} />

export const Template = (args) => <Badge {...args} />;

# 📛 Badge

`Badge` is used to display brief status-related information. For longer text, please use `Notice`.

The most common application for a `Badge` is in a list item, to display the status of an asset, event, or user, or to denote that something is new or is behind an updgraded plan paywall. `Badge` should not be pressable, but can be part of a pressable containing other items.

## Usage

```jsx
import Badge from 'components/shared/DesignSystem/SlabTransferPortal/Badges/Badge';

---

<Badge iconName="account-check">I'm a Badge</Badge>
```

<Canvas>
  <Story
    name="default"
    args={{
      children: "I'm a badge",
      iconName: "account-check"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

## Variations

<Canvas>
  <Story
    name="regular"
    args={{
      children: "Invited",
      iconName: "account-check",
      color: "success",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="upgrade"
    args={{
      children: "Upgrade",
      iconName: "lightning-bolt-circle",
      color: "upgrade",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="connected"
    args={{
      children: "Connected",
      iconName: "link-variant",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="pending"
    args={{
      children: "Signature Pending",
      iconName: "file-sign",
      color: "caution",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="deactivated"
    args={{
      children: "Deactivated",
      iconName: "account-off",
      color: "destroy",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="add-on"
    args={{
      children: "Add-On",
      iconName: "plus-circle-outline",
      color: "add_on",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="assigned"
    args={{
      children: "Assigned",
      iconName: "account-check",
      color: "assigned",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="regular-small"
    args={{
      children: "Invited",
      iconName: "account-check",
      color: "success",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="upgrade-small"
    args={{
      children: "Upgrade",
      iconName: "lightning-bolt-circle",
      color: "upgrade",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="connected-small"
    args={{
      children: "Connected",
      iconName: "link-variant",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="pending-small"
    args={{
      children: "Signature Pending",
      iconName: "file-sign",
      color: "caution",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="deactivated-small"
    args={{
      children: "Deactivated",
      iconName: "account-off",
      color: "destroy",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="add-on-small"
    args={{
      children: "Add-On",
      iconName: "plus-circle-outline",
      color: "add_on",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="assigned-small"
    args={{
      children: "Assigned",
      iconName: "account-check",
      color: "assigned",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## `BadgeNew` Variation
We will be including a `BadgeNew` in the folder structure to use for when you need to call out a new feature. This should always be the same size (`small`), color (`caution`), and text, so baking everything into a component (including translations for the "New" text) that extends the regular `Badge` component makes sense in this case for ease of use and DRY behavior.

```jsx
import BadgeNew from 'components/shared/DesignSystem/SlabTransferPortal/Badges/BadgeNew';

<BadgeNew />
```

<Canvas>
  <Story
    name="new"
    args={{
      children: "New",
      color: "caution",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>