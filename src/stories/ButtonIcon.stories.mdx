import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import ButtonIcon from "../components/ButtonIcon";
import Button from "../components/Button";

<Meta title="ButtonIcon" component={ButtonIcon} subcomponents={{ Button }} />

export const Template = (args) => <ButtonIcon {...args} />;

# 🔵 ButtonIcon

A `<Button />` extended to render a single icon. Note that the `accessibilityLabel` prop is required.

## Usage

```jsx
import ButtonIcon from 'components/shared/DesignSystem/SlabTransferPortal/Buttons/ButtonIcon';

---

<ButtonIcon accessibilityLabel="Hi" color="primary" iconName="account-multiple" />
```

## Props Playground

A `<Button>` extended to have the same width/height and support a single icon.

<Canvas>
  <Story
    name="default"
    args={{
      accessibilityLabel: "Hi",
      size: "medium",
      color: "primary",
      minimal: false,
      shape: "circle",
      iconName: "account-multiple",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
