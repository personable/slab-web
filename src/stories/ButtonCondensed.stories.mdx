import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import ButtonCondensed from "../components/ButtonCondensed";
import Button from "../components/Button";

<Meta
  title="ButtonCondensed"
  component={ButtonCondensed}
  subcomponents={{ Button }}
/>

export const Template = (args) => <ButtonCondensed {...args} />;

# 🟦 ButtonCondensed

A minimal `<Button />` extended for situations where you don't want the default Button padding.

## Usage

```jsx
import ButtonCondensed from 'components/shared/DesignSystem/SlabTransferPortal/Buttons/ButtonCondensed';

---

<ButtonCondensed color="primary">
  Condensed Button
</ButtonCondensed>
```

## Props Playground

<Canvas>
  <Story
    name="default"
    args={{
      children: "Condensed Button",
      size: "medium",
      color: "primary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
