import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import ButtonCondensed from "../components/ButtonCondensed";
import Button from "../components/Button";

<Meta
  title="ButtonCondensed"
  component={ButtonCondensed}
  subcomponents={{ Button }}
/>

export const Template = (args) => <ButtonCondensed {...args} />;

# ButtonCondensed

A minimal Button extended as a styled component for where you don't want padding

<Canvas>
  <Story
    name="default"
    args={{
      children: "Condensed Button",
      size: "medium",
      color: "primary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
